# meme-server

## usage
运行前在根目录创建 `app.config.js` (与 `src/` 同级)
``` javascript
module.exports = {
  // 对象存储器信息
  AppId: '', 
  SecretId: '',
  SecretKey: '',
  Region: '',

  port: 3001
}
```